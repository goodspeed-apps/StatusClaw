# StatusClaw Environment Configuration

# Email Configuration (Resend)
# Email notifications are disabled by default unless RESEND_API_KEY is set
RESEND_API_KEY=re_your_resend_api_key_here
EMAIL_FROM_ADDRESS=notifications@statusclaw.com

# OpenRouter API Key - for real-time spend tracking (optional)
OPENROUTER_API_KEY=your_openrouter_key_here

# Gemini API Key - for image generation (optional)
GEMINI_API_KEY=your_gemini_key_here

# Auth credentials for debug/admin endpoints (optional, defaults used if not set)
AUTH_USER=admin
AUTH_PASSWORD=changeme

# API URL for client-side requests (optional)
NEXT_PUBLIC_API_URL=
